if(first[v->val])
            // {
            //     prev = v;   
            // }
            // else prev->next = v;
            // if(end[v->val])
            // {
            //     v->next = nullptr;
            // }