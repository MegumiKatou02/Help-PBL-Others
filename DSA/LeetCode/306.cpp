#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    bool isAdditiveNumber(string num) {
        for(int i = 1; i < num.length() - 1; i++)
        {
            int x = num[i - 1] - '0';
            int y = num[i] - '0';
            int z = num[i + 1] - '0';
        }
    }
};

int main()
{

    return 0;
}